specVersion: "v1"
serviceId: "runalert"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "runalert-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
